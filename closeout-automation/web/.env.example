# Required for AI drafting (workmanship warranty, care guide, cover letter,
# outstanding-items email, moisture-report extraction). Without this set,
# the app runs fine but AI buttons return a "not configured" message.
ANTHROPIC_API_KEY=

# Optional: override the Claude model used for drafting. Defaults to
# claude-sonnet-5 if unset.
ANTHROPIC_MODEL=
